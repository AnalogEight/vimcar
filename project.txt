TODO:
X layout header
X layout products
X create 'add to basket' component which updates number
X expand basket component to check for availibility

EXTRA:
- add Alert component (css and js)
- animate product hover
X generate list of products from JSON file (REST-API wise) instead of hard-coded (backend render)

JS COMPONENTS:
basket
 X Update number to represent items in basket
 X check if item is available 
 X add to basket functionality

product
 - generate list of products from JSON file

CSS COMPONENTS
 X header
 X product 
 - animations (extra: Add basket number animations)